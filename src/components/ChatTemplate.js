import React from 'react';
import styled from 'styled-components';
import '../App.css';


function ChatTemplate({ children }) {
  return <section className="avenue-messenger" >{children}</section>;
}

export default ChatTemplate;
